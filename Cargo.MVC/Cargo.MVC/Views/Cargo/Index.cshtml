@* @model List<CargoViewModel>

<div class="mt-2 mb-2">
    <a asp-controller="Cargo" asp-action="New" class="btn btn-warning">Cargo Oluştur</a>
</div>

<div class="row justify-content-center">
    <div class="col-8">

        <table class="table">

            <tr>
                <th>CargoNo</th>
                <th>TelNo</th>
                <th>SenderFirstName</th>
                <th>SenderLastName</th>
                <th>Status</th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.CargoNo </td>
                    <td>@item.SenderTelNo</td>
                    <td>@item.SenderFirstName</td>
                    <td>@item.SenderLastName</td>
                    <td>@item.Status</td>
                    <td><a class="btn btn-primary" asp-controller="Cargo" asp-action="Edit" asp-route-id="@item.Id">Güncelleştir </a>
                    <a class="btn btn-danger" asp-controller="Cargo" asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                    </td>
                    
                </tr>
            }
        </table>

    </div>
    </div> *@
@model List<CargoViewModel>


<style>
    body {
        /*  font-family:"Open Sans",sans-serif; */
        background-color: #e84242;
    }

    table {
        width: 900px;
        margin: 0px auto;
        border-collapse: collapse;
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0px 0px 20px black;
        margin-bottom: 40px;
        margin-top: 40px;
    }

    thead {
        background-color: black;
        line-height: 50px;
        text-align: left;
        color: white;
    }

        thead tr th:first-child {
            padding-left: 40px;
        }

    tbody {
        line-height: 45px;
    }

        tbody tr td:first-child {
            padding-left: 40px;
        }

        tbody tr:nth-child(even) {
            background-color: white;
            color: black;
        }

        tbody tr {
            transition: all 0.5s ease;
            background-color: white;
            color: black;
        }
        thead  a{
        font-size:50px;
        }

    tbody a {
        background-color: black;
        line-height: 50px;
        text-align: left;
        color: white;
    }

            tbody tr:hover {
                background-color: black;
                color: white;
                cursor: pointer;
            }

</style>



<table>
    <thead>
        <tr>
            <th>CargoNo</th>
            <th>TelNo</th>
            <th>SenderFirstName</th>
            <th>SenderLastName</th>
            <th>Status</th>
            <th> <a asp-controller="Cargo" asp-action="New" style="color:white;font-size:15px; " class="btn">Yeni Kargo Oluştur</a></th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CargoNo</td>
                <td>@item.SenderTelNo</td>
                <td>@item.SenderFirstName</td>
                <td>@item.SenderLastName</td>
                <td>@item.Status</td>
                <td>
                    <a style="font-size:15px" class="btn btn-warning" asp-controller="Cargo" asp-action="Edit" asp-route-id="@item.Id">Güncelleştir </a>
                    <a style="font-size:15px" class="btn btn-danger" asp-controller="Cargo" asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                </td>
            </tr>
        }

    </tbody>
    <tfoot></tfoot>
</table>












@* @{

    Layout = null;
}
 *@
@* <html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@1,500&display=swap" rel="stylesheet">
    <style>
         body{
          /*  font-family:"Open Sans",sans-serif; */
            background-color: #e84242;

         }
         table{
             width:900px;
             margin:0px auto;
             border-collapse:collapse;
             border-radius:20px;
             overflow:hidden;
             box-shadow:0px 0px 20px black;
             margin-bottom:40px;
             margin-top:40px;
         }
        thead{
            background-color: black;
            line-height:50px;
            text-align:left;
            color:white;
        }
        thead tr th:first-child{
            padding-left:40px;

        }
        tbody{
            line-height:45px;

        }
        tbody tr td:first-child{
      
            padding-left:40px;
        }
        tbody tr:nth-child(even){
            background-color:white;
            color:black;
        }
        tbody tr{
            transition:all 0.5s ease;
                background-color: white;
                color: black;
        }
        tbody tr:hover{
            background-color:black;
            color:white;
            cursor:pointer;
        }
       
        
    </style>
</head>
<body>
            <table  >
                <thead>
                   <tr>
                <th>CargoNo</th>
                <th>TelNo</th>
                <th>SenderFirstName</th>
                <th>SenderLastName</th>
                <th>Status</th>
                <th> <a asp-controller="Cargo" asp-action="New" style="color:white" class="btn">Yeni Kargo Oluştur</a></th>
                  </tr>
                </thead>
                <tbody>

            @foreach(var item in Model)
            {
               <tr>
                        <td>@item.CargoNo</td>
                        <td>@item.SenderTelNo</td>
                        <td>@item.SenderFirstName</td>
                        <td>@item.SenderLastName</td>
                        <td>@item.Status</td>
                    <td>
                        <a class="btn btn-warning" asp-controller="Cargo" asp-action="Edit" asp-route-id="@item.Id">Güncelleştir </a>
                        <a class="btn btn-danger" asp-controller="Cargo" asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                    </td>
                </tr>  
            }
           
                </tbody>
                <tfoot></tfoot>
            </table>               

</body>
</html> *@










@* <div class="mt-4 mb-4 " style="text-align:center;margin-right:20px; ">
    <a asp-controller="Cargo" asp-action="New" class="btn btn-warning">Yeni Cargo Oluştur</a>
</div>


<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <table class="table table-stipedd">
                <thead>
                    <tr>
                        <th >Cargo No</th>
                        <th >Tel No</th>
                        <th >Gönderici Adı</th>
                        <th >Gönderici Soyadı</th>
                        <th>Durum</th>
                        <th >İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.CargoNo</td>
                            <td>@item.SenderTelNo</td>
                            <td>@item.SenderFirstName</td>
                            <td>@item.SenderLastName</td>
                            <td>@item.Status</td>
                            <td>
                                <a class="btn btn-primary" asp-controller="Cargo" asp-action="Edit" asp-route-id="@item.Id">Güncelleştir</a>
                                <a class="btn btn-danger" asp-controller="Cargo" asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    th {
        background-color: red;
        color: white;
    }
</style> *@


@* <div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Cargo No</th>
                        <th>Tel No</th>
                        <th>Gönderici Adı</th>
                        <th>Gönderici Soyadı</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.CargoNo</td>
                            <td>@item.SenderTelNo</td>
                            <td>@item.SenderFirstName</td>
                            <td>@item.SenderLastName</td>
                            <td>@item.Status</td>
                            <td>
                                <a class="btn btn-primary" asp-controller="Cargo" asp-action="Edit" asp-route-id="@item.Id">Güncelleştir</a>
                                <a class="btn btn-danger" asp-controller="Cargo" asp-action="Delete" asp-route-id="@item.Id">Sil</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div> *@